root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/agentbox"
  bin = "./tmp/main"
  include_ext = ["go", "toml", "yaml"]
  exclude_dir = ["tmp", "bin", "web", "node_modules", "docker"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
