# AgentBox

**AI Agent 云交付平台**

将 Claude Code、Codex、OpenCode 等专业 AI Agent，通过可配置的方式，以 Docker 容器稳定交付给用户。

## 定位

```
用户只需要：提交任务 + 选择 Profile → 获取结果

AgentBox 负责：Agent 管理 + 环境配置 + 容器运行 + 能力扩展
```

AgentBox **不是**另一个 AI Agent，而是 **AI Agent 的运行平台**。

| 对比 | AgentBox | Manus |
|------|----------|-------|
| Agent | 托管已有 Agent | 自研 Agent |
| 定位 | 纯平台层（开放生态） | Agent + 平台（软硬一体） |
| 扩展性 | 可接入任意 Agent | 只能用 Manus Agent |
| 部署 | 开源可自托管 | 闭源 SaaS |

## 核心价值

| 价值 | 说明 |
|------|------|
| **Agent 无关** | 不绑定特定 Agent，支持 Claude Code、Codex、OpenCode 等 |
| **可配置增强** | 通过 Profile 组合 MCP Servers + Skills + Credentials |
| **容器化交付** | Docker 隔离，安全、稳定、可扩展 |
| **统一 API** | 一套 API 调用所有 Agent |
| **开源自托管** | 企业可私有部署，数据不出境 |

## 概念模型

```
Agent         底层执行引擎（Claude Code / Codex / OpenCode）
    │
    ▼
Profile       Agent 配置模板 = Agent + MCP + Skill + Credential + 资源限制
    │
    ▼
Task          用户提交的任务 = Profile + Prompt + Input
    │
    ▼
Session       运行中的实例 = Docker 容器
    │
    ▼
Output        任务执行结果 = 文件 + 日志 + 元数据
```

## 为什么是这些 Agent？

### 编码 Agent，通用能力

Claude Code、Codex 等工具最初为编码而生，但它们的核心能力远超编码本身：

| 能力 | 编码场景 | 通用场景 |
|------|----------|----------|
| **文件读写** | 读取代码、写入代码 | 读取文档、生成报告 |
| **命令执行** | 运行测试、构建项目 | 执行脚本、调用 API |
| **上下文理解** | 理解代码库 | 理解任意文件结构 |
| **多轮交互** | 迭代修复 bug | 迭代完成复杂任务 |
| **工具调用** | 调用 linter、formatter | 调用任意 MCP 工具 |

> *"Claude Code has become far more than a coding tool. At Anthropic, they've been using it for deep research, video creation, and note-taking, among countless other non-coding applications."*
> — [Anthropic Engineering](https://www.anthropic.com/engineering/claude-code-best-practices)

### 支持的 Agent

| Agent | 官方描述 | 状态 |
|-------|----------|------|
| **Claude Code** | Anthropic 的 agentic coding 工具，在终端运行，理解代码库，通过自然语言命令执行任务。设计哲学是"给 Claude 一台电脑"。 | ✅ 已支持 |
| **Codex CLI** | OpenAI 的轻量级编码代理，本地运行于终端，可读取、修改和运行代码。开源，用 Rust 构建。 | ✅ 已支持 |
| **OpenCode** | 开源代码 Agent | 🔜 计划中 |
| **Aider** | AI Pair Programming | 🔜 计划中 |

### Agent SDK 生态

除了直接使用 Agent，还可以通过 SDK 构建自定义 Agent：

| SDK | 说明 | 典型用例 |
|-----|------|----------|
| **Claude Agent SDK** | 基于 Claude Code 的能力构建自定义 Agent。提供相同的工具、agent loop 和上下文管理。 | 金融分析、个人助理、客服系统 |
| **OpenAI Agents SDK** | 轻量级 Python 框架，用于编排 agentic 工作流。四个核心原语：Agents、Handoffs、Guardrails、Tracing。 | 多 Agent 协作、语音 Agent、工作流自动化 |

**AgentBox 的价值**：无论使用哪种 Agent 或 SDK，AgentBox 提供统一的容器化运行环境、配置管理和任务调度。

## 快速开始

### 前置要求

- Go 1.21+
- Docker Desktop
- Node.js 18+ (Web UI)

### 1. 构建 Agent 镜像

```bash
git clone https://github.com/tmalldedede/agentbox.git
cd agentbox

# 构建 Agent 镜像
docker build -t agentbox/agent:latest -f docker/agent/Dockerfile .
```

### 2. 运行后端

```bash
# 构建
make build

# 运行（默认端口 18080）
./bin/agentbox

#找到占用 18080 端口的进程 PID 并杀掉它
Stop-Process -Id (Get-NetTCPConnection -LocalPort 18080).OwningProcess -Force

#启动后端
go run cmd/agentbox/main.go
```

### 3. 运行 Web UI

```bash
cd web
npm install
npm run dev
# 访问 http://localhost:5173
```

### 4. API 文档

启动后端后，访问 Swagger UI 查看完整 API 文档：

```
http://localhost:18080/swagger/index.html
```

## 测试

### 单元测试

```bash
# 运行所有 API 测试
go test -v ./internal/api/...

# 运行特定测试
go test -v ./internal/api/... -run TestProvider
```

### 集成测试 (Hurl)

```bash
# 安装 Hurl
brew install hurl

# 运行集成测试（需要后端运行）
hurl --test tests/integration/api.hurl --variable host=http://localhost:18080
```

测试覆盖：
- Health、Agents API
- Provider CRUD + 过滤
- Profile CRUD + Clone
- Task CRUD + 元数据
- Webhook CRUD

## API 概览

### 对外服务 API（Task API）

```bash
# 创建任务
POST /v1/tasks
{
  "profile": "claude-code-default",
  "prompt": "分析代码架构，生成文档",
  "input": {"type": "git", "url": "https://github.com/user/repo"}
}

# 获取任务状态
GET /v1/tasks/{task_id}

# 获取任务输出
GET /v1/tasks/{task_id}/output

# Webhook 回调通知任务完成
POST /v1/webhooks
```

### 管理 API

```bash
# Profile 管理
GET/POST/PUT/DELETE /v1/profiles

# MCP Server 管理
GET/POST/PUT/DELETE /v1/mcp-servers

# Skill 管理
GET/POST/PUT/DELETE /v1/skills

# Credential 管理
GET/POST/PUT/DELETE /v1/credentials

# 系统监控
GET /v1/system/health
GET /v1/system/stats
```

## 架构

```
                              用户 / 应用
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                          API Gateway                             │
└─────────────────────────────────────────────────────────────────┘
                                   │
┌─────────────────────────────────────────────────────────────────┐
│                        Task Scheduler                            │
│               队列管理 │ 优先级调度 │ 并发控制                     │
└─────────────────────────────────────────────────────────────────┘
                                   │
┌─────────────────────────────────────────────────────────────────┐
│                       Profile Resolver                           │
│           解析 Profile → 组装 Agent + MCP + Skill + Cred         │
└─────────────────────────────────────────────────────────────────┘
                                   │
┌─────────────────────────────────────────────────────────────────┐
│                      Container Manager                           │
│                  Docker SDK │ 容器生命周期管理                    │
└─────────────────────────────────────────────────────────────────┘
                                   │
         ┌─────────────────────────┼─────────────────────────┐
         ▼                         ▼                         ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Claude Code    │     │     Codex       │     │   OpenCode      │
│   Container     │     │   Container     │     │   Container     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

## 目录结构

```
agentbox/
├── cmd/agentbox/           # 主程序入口
├── internal/
│   ├── api/                # HTTP API
│   ├── agent/              # Agent 适配器
│   ├── container/          # Docker 容器管理
│   ├── session/            # 会话管理
│   ├── profile/            # Profile 管理
│   ├── mcp/                # MCP Server 管理
│   ├── skill/              # Skill 管理
│   ├── credential/         # Credential 管理
│   └── config/             # 配置管理
├── docker/                 # Dockerfile
├── web/                    # Web 管理界面
└── docs/                   # 文档
    └── VISION.md           # 产品愿景
```

## 文档

- [产品愿景](./docs/VISION.md) - 定位、价值主张、目标用户
- [架构设计](./DESIGN.md) - 技术架构、模块设计
- [API 参考](./docs/reference/) - API 文档

## 路线图

- [x] **Phase 1**: Agent 适配器、Profile、MCP、Skill、Credential
- [x] **Phase 2**: Task API、任务调度、Webhook、Provider 系统、Swagger 文档
- [ ] **Phase 3**: 多租户、认证、计费
- [ ] **Phase 4**: 更多 Agent、SDK、GitHub 集成

## 许可证

[Apache-2.0](./LICENSE)
